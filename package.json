{
  "name": "catalogue",
  "workspaces": [
    "web-app"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present"
  },
  "private": true,
  "devDependencies": {
    "eslint": "^9.13.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
